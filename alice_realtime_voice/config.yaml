name: "Alice Realtime Voice"
version: "0.4.0"
slug: "alice_realtime_voice"
description: "Yerel STT + endpointing/VAD eventleri + temel HA bridge komutlari olan realtime voice pipeline"
startup: "application"
boot: "manual"
init: false
homeassistant_api: true
arch:
  - amd64
ports:
  8766/tcp: 8766
ports_description:
  8766/tcp: "Alice realtime voice WebSocket"
options:
  port: 8766
  debug_logs: true
  stt:
    provider: "faster_whisper"
    model: "small"
    language: "tr"
    compute_type: "int8"
    beam_size: 1
    vad_filter: true
  endpointing:
    enabled: true
    auto_finalize_on_vad_end: false
    no_speech_timeout_ms: 5000
    speech_start_min_ms: 160
    speech_end_silence_ms: 700
    max_utterance_ms: 15000
    start_avg_abs_threshold: 220
    end_avg_abs_threshold: 140
  ha_bridge:
    enabled: true
    api_base_url: "http://supervisor/core/api"
  llm:
    provider: "openai"
    model: "gpt-5-mini"
    api_key: ""
    base_url: ""
    system_prompt: ""
  tts:
    enabled: false
    relay_url: "ws://127.0.0.1:8765/ws"
schema:
  port: port
  debug_logs: bool
  stt:
    provider: list(faster_whisper)
    model: str
    language: str
    compute_type: str
    beam_size: int
    vad_filter: bool
  endpointing:
    enabled: bool
    auto_finalize_on_vad_end: bool
    no_speech_timeout_ms: int
    speech_start_min_ms: int
    speech_end_silence_ms: int
    max_utterance_ms: int
    start_avg_abs_threshold: int
    end_avg_abs_threshold: int
  ha_bridge:
    enabled: bool
    api_base_url: str
  llm:
    provider: list(openai|openrouter|none)
    model: str
    api_key: password
    base_url: str
    system_prompt: str
  tts:
    enabled: bool
    relay_url: str
